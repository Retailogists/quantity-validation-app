{
  "name": "quantity-validation-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.js",
    "setup-metafields": "node scripts/setup-metafields.js",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "scaffold": "shopify app scaffold",
    "deploy": "shopify app deploy",
    "sync-inventory": "node scripts/sync-inventory.js"
  },
  "dependencies": {
    "@shopify/cli": "3.72.1",
    "node-cron": "^3.0.3",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {},
  "workspaces": [
    "extensions/*"
  ]
}
