query RunInput {
  buyerJourney {
    step
  }
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          id
          title
          # Use the metafield format that was working in the logs
          inventoryMetafield: metafield(namespace: "custom", key: "inventory") {
            value
          }
          product {
            id
            title
          }
        }
      }
    }
  }
} 